// like window object in browser global object is used inside the terminal

//we can use same function in window object such as setTimeOut setInterval excluding some operation which involves dom manipulation like document etc.

// setTimeout(() => {
//   console.log('in timeout')
//   clearInterval(inter)
// }, 4000)

// const inter = setInterval(() => {
//   console.log('in interval')
// }, 1000)

// //we can access absolute path of current file and directory

// console.log(__filename)
// console.log(__dirname)
